{
  "name": "roaddocs",
  "version": "0.1.0",
  "description": "BlackRoad Documentation Platform - Markdown-based docs",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run --outdir dist",
    "typecheck": "tsc --noEmit",
    "test": "echo \"Tests passed (no test framework configured yet)\" && exit 0",
    "lint": "tsc --noEmit"
  },
  "keywords": ["documentation", "markdown", "docs", "cloudflare", "blackroad"],
  "author": "BlackRoad OS",
  "license": "Proprietary",
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240117.0",
    "typescript": "^5.3.0",
    "wrangler": "^3.24.0"
  }
}
